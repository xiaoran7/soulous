# 测试环境覆盖（.properties 优先级高于主 classpath 的 application.yml，二者共存不互相遮蔽）。
# 单测必须与 agent-service 边车解耦：开发机上若恰好有活的 agent 在 8100 监听，
# ChatServiceTests 等会被它劫持（脚本化 LLM 队列没人消费、每次调用还要吃满超时），
# 表现为测试又慢又随机挂。测试只验证本地降级路径，agent 链路由 agent-service 自己的 pytest 覆盖。
soulous.agent.enabled=false
